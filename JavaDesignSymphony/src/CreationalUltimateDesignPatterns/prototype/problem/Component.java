package CreationalUltimateDesignPatterns.prototype.problem;

//Component, because in additional to shapes, we can also add a text component, images and so on...
public interface Component {
    void render();
}
